# User contributions directory

All user provided contributions should be placed in this directory with the
ingame `accountname` as a subdirectory.

So a user with an account named `tom` should create a subdirectory `users/tom/`
to place his files in (don't forget the `__init__.py`). You can also create
subdirectories below these user directories like Objects, Rooms or Scripts.
